Canonical character reference images: <slug>.png matching config/characters.yaml.
Design once (Midjourney, GPT-4o, SDXL...), save here, commit — the pipeline
conditions every frame the character appears in on this image via IP-Adapter.
Re-rendering after changing a reference is automatic (frame cache keys on it).
